#!/usr/bin/env node

console.error(
  '[DEPRECATED] The Node CLI prototype is no longer a production metadata source.\n' +
    'Use the WordPress plugin (wp-adobe-stock-metadata) as the single source of truth for Adobe Stock compliance.'
);
process.exit(2);
